# 1) editas data\aspiradores.yaml
# 2) generas stubs que falten
Flujo correcto a partir de ahora (YAML como única fuente)

Editas solo data/aspiradores.yaml

Regeneras stubs:

python tools/sync_ofertas.py
python tools\generar.py --force --clean-modelos


git status
git diff
git add layouts/partials/model_crosslinks.html layouts/modelos/recambio.html layouts/modelos/problema.html
git diff --staged
git commit -m "Add model cross-linking blocks (recambio/problema)"
git push
